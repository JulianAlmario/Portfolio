---

interface Props{
    title:string;
    link:string;
}
const {title, link} = Astro.props as Props;
---

<button class="cursor-pointer bg-azure-600 hover:bg-azure-700 transition duration-100 text-white rounded-lg p-6 py-2">
  <a href={link} download="Julian Almario CV">{title}</a>
</button>